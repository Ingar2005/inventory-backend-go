create a .env file in the root directory and add the following variables
```
DB_USER=
DB_PASSWORD=
DB_ENDPOINT=
DB_NAME=
```
note: endpoint should be the endpoint of your RDS instance including the port number
